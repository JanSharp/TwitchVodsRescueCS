
If you've downloaded the source code of this project you can put the csv file and collection files into this configuration folder. Those files are git ignored so they won't be any bother. This makes it easier to use `dotnet run` directly.
